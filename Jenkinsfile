pipeline {
    agent any
    stages {
        stage( 'clone repo') {
            steps {
                sh "git status"
                sh " git clone https://github.com/thakurpractise/learnings.git "
            }
        }
    }
}
